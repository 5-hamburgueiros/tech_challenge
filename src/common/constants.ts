export const IGNORED_PATHS = [/hc$/, /liveness$/, /swagger/m, /queues/m];

export const QUEUE_TIMEOUT = {
  THIRTY_SECONDS: 30000,
};

export const DEFAULT_JOB_NAME = '__default__';
export const APP_NAME = 'Tech Challenge';
